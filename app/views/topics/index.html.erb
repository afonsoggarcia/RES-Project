<%= render 'shared/navbar' %>
<h1>Topics</h1>
  <br/>
  <% @topics.each do |topic| %>
  <div class="cards m-4 p-4 text-center">
  <div>
    <p>Topic by.: <strong><%= topic.user.nickname%></p></strong>
  <%= link_to topic_path(topic), class:'text-decoration-none w-100 text-dark' do %>
    <h2> <%= topic.title%> </h2></div>
    <br/>
      <%end%>
  </div>
    <%end%>
